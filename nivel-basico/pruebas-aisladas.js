//Función que muestra arrays por pantalla
const muestraArrays = arreglo =>{
    for (let index = 0; index < arreglo.length; index++) {
     console.log(arreglo[index]);
 } 
 }
 //Función que recibe un array como parámetro y devuelve otro array con los números enteros
const esEntero = (arrayParaFiltrar, arrayVacio) =>{arrayParaFiltrar.filter(posicionArray=> (posicionArray%2 ==0)arrayVacio.push(posicionArray);
    }
    return arrayVacio;
};

let arrayNumEnteros= [];
let arraji = [4, 6, 6, 7, 1];
esEntero(arraji, arrayNumEnteros);
muestraArrays(arrayNumEnteros);